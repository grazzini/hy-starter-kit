{
  "scripts": {
    "dev": "vuepress dev --host localhost --port 8081",
    "build": "vuepress build",
    "algolia": "cd .vuepress/docsearch-scraper && ./docsearch docker:run ../algolia-scraper.config.json"
  }
}
